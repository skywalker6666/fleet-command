# Agent Log — 2026-04-20

---

## CardSense — Supabase Discord 回饋通知系統完成

- **任務**: 串接 Feedback Widget → Supabase → Discord 即時通知
- **結果**: ✅ 成功，Discord 收到完整 embed 含截圖

### 完成項目

1. **Supabase Storage Bucket**
   - 建立 `feedback-images` public bucket
   - 設定 `Allow anonymous uploads` policy（INSERT for anon）

2. **Supabase Edge Function `notify-discord`**
   - 接收 Database Webhook payload，轉發 Discord embed
   - 支援截圖圖片（`context.screenshot_url` 有值才附上 image field，避免 Discord 拒絕空 URL）
   - 環境變數：`DISCORD_WEBHOOK_URL`

3. **Supabase Database Webhook**
   - Table: `feedbacks`，Event: `Insert`
   - Type: `Supabase Edge Functions` → `notify-discord`
   - 注意：不使用 HTTP Parameters 方式，原因為 Supabase UI bug（編輯時內容消失）且格式不符 Discord 規範

### Discord 通知 Embed 欄位
- 類型 (Type)
- 回報畫面 URL（可點擊連結）
- 使用者描述
- 截圖圖片（選填）

### 相關文件
- `fleet-command/docs/Supabase-Discord-Webhook-Setup.md` — 已更新為 Edge Function 方案
